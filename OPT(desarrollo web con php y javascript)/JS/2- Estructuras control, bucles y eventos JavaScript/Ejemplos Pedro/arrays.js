// Vamos a crear un Vector (arrays de 1 dimension)// Matriz = arrays bidimensional = matriz [] [].
let pruebaVec = []; // Aqui le estamos pasando un vector vacio.

function pulsoTeclas(){
    alert("Has pulsado una tecla");
}
function mouseOut(){
    alert("El raton se va")
}

// Para recoger informacion de forma agrupada se usara:
// getElementByClass(varios elementos misma clase) y getElementById(id unica)